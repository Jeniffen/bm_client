import React from 'react';
import { Navbar } from './../../components/Navbar';

const Wishlist = ({ navbarItem }) => {
  return (
    <>
      <Navbar navbarItem={navbarItem} />
    </>
  );
};

export default Wishlist;
