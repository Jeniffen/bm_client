import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Tile } from '.';

<Meta title="Components/Tile" component={Tile} />

# Tile

This `Tile` compoent is designed to be responsive for devices with a max with of
`768px`. This component can be used stand-alone or in conjunction with a
`Carousell` compoent with multiple `Tile` components as children.

export const Template = (args) => <Tile {...args} />;

<Canvas withToolbar>
  <Story
    name="Default"
    args={{ imageURL: 'https://bit.ly/3EhYqSc', label: 'Photography' }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Tile} />
