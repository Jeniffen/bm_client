import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { Carousell } from '.';
import { Tile } from '../Tile';

<Meta title="Components/Carousell" component={Carousell} />

# Carousell

export const Template = (args) => <Carousell {...args} />;

<Canvas withToolbar>
  <Story name="Default" args={{ header: 'Explore services for your event' }}>
    {Template.bind({})}
  </Story>
</Canvas>
